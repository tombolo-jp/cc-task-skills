# 検証手順書

## 検証前提

```yaml
# task-verify-run が最初に読み取る機械可読ブロック。
# 値が未確定のキーは削除せず null のままにする。
base_url: [http://localhost:3000 / null]
start_command: [アプリ起動コマンド / "none" / null]
stop_command: null
test_command: [テスト実行コマンド / null]
test_accounts:            # null = 未確定（必ず対話で確認）／[] = 認証不要
  - role: [admin]
    username: [ユーザー名]
    password: env:[環境変数名]
environment:
  type: null
  db_backup_command: null
  db_restore_command: null
  mail_guard_command: null
reset_commands: []
```

## 検証概要
- タスク名: $ARGUMENTS[0]
- 作成日時: [日時]
- 検証対象: [実装された機能の概要を1-2行で]
- 検証項目数: [N]件

## 環境準備

- [ ] `V-001` [準備のタイトル]
  - 操作: [手順]
  - 期待結果: [具体的な状態]

## Quick Win

- [ ] `V-002` [最短で結果が確認できるケースのタイトル]
  - 操作: [手順]
  - 期待結果: [具体的な値・表示]

## 機能検証: [機能名]

- [ ] `V-003` [ケースのタイトル]
  - 前提: [先行して合格が必要な V-ID / 状態]
  - 操作:
    1. [完全形 URL を開く]
    2. [クリック・入力等]
  - 入力: [キー] = [値]
  - 期待結果: [具体的な文字列・数値・HTTP ステータス・DOM の状態]
  - 自動化メモ: [セレクタ・待機条件・判定箇所]

- [ ] `V-004` [コマンドを伴うケースのタイトル]
  - 操作: 下記コマンドを実行する

    ```bash
    [コピペで実行できるコマンド]
    ```

  - 期待結果: [終了コード・出力に含まれる文字列]

## 異常系・エッジケース

- [ ] `V-005` [ケースのタイトル]
  - 操作: [不正な入力・境界値の投入手順]
  - 入力: [キー] = [値]
  - 期待結果: [エラーメッセージの文言・HTTP ステータス]

## 非機能・回帰

- [ ] `V-006` [ケースのタイトル]
  - 操作: [手順]
  - 期待結果: [具体的な閾値・既存機能が動作する条件]

## 人手確認（自動化不可）

- [ ] `V-007` [人の目でしか判定できない確認のタイトル]
  - 操作: [手順]
  - 期待結果: [どこを見て何を判定するか]

<!--
以下は task-verify-run が実行後に追記するセクション。生成時には出力しない。

## スキップした項目

- `V-000` [ケースのタイトル] — 軸[n]（[軸名]）: [1行の具体理由]
-->

<!--
以下は --manual 指定時のみ使用する形式。既定（詳細版）では出力しない。

## 検証概要 に1行だけ加える:
- 想定所要時間: ⏱ 約[X]分（全[N]項目）

## Quick Win
- [ ] `V-002` [操作] → [期待結果]
- [ ] `V-003` [操作]

## 周辺確認（異常系・非機能・回帰）
- [ ] `V-005` [人の目でしか判定できない確認] → [期待結果]

## 発見した問題（該当する場合）
| # | 内容 | 重大度 | 該当箇所 |
|---|------|--------|---------|
| 1 | [問題の内容] | [高/中/低] | [V-ID / セクション名] |
-->

## メタ情報
- 実行モデル: [モデル名]
- 実行日時: [日時]
- Agent Teams: [有効 / 無効（指定なし） / 無効（フォールバック）]

# 開発完了報告書

## プロジェクト概要
- タスク名: $ARGUMENTS[0]
- 開発開始日時: [開始日時]
- 開発完了日時: [完了日時]
- レビュー体制: [同期 Agent 委譲（[n] 体） / 自己レビュー（degrade: [理由]） / レビュー未実施（委譲不成立）]
- 委譲機構: 同期 Agent

## タスク完了状況
[design.md の実装タスク一覧（T-nnn）から抽出したチェックリスト。todo.md がある場合はその項目も含む]

## 実装概要
[実装した機能の概要]

## 変更ファイル一覧
<!-- W-5: 追記。レビュー修正で触れたファイルも足し、コミット対象の和集合と一致させる -->
### 新規作成ファイル
### 修正ファイル

## 技術的な実装詳細

## パフォーマンスとセキュリティ

## 既知の課題・改善点

## 動作確認結果

## レビュー指摘一覧
<!-- BC-3: 旧 review.md / fix-result.md を検出した場合は、その事実をこの直下へ HTML コメントで1行残す -->
<!-- W-3: 新規 R-nnn は末尾追記のみ。既存項目で書き換えてよいのは
     ①チェックボックス（再レビューで解消を確認したときのみ [x]。修正した時点では付けない）
     ②`- 対応:` 行 の2箇所に限る。
     W-4: `- 対象:` / `- 根拠:` / `- 署名:` / 見出し行の 優先度・区分・類型 は変更禁止。
     区分を引き下げる場合は既存値を残したまま `- 区分変更: 必須 → 推奨（J-n: 根拠）` 行を追加する。
     `- 対応:` の固定語彙（この7種のみ）:
       未着手 / 修正済（ループN） / 修正済（ループN）→ 再レビュー合格（ループM） /
       記録のみ（推奨のため自動修正しない） / 保留（要判断）・保留（安全境界）・保留（スコープ外） /
       却下（J-n: 根拠） / 打ち切り（修正予算超過）・未着手（修正予算超過） -->

- [ ] `R-001` 優先度: [高 / 中 / 低] ／ 区分: [必須 / 推奨] ／ 類型: [欠陥類型の9値] — [1行要約]
  - 対象: [リポジトリ相対パス または パス#シンボル名。行番号は書かない]
  - 根拠: [要件 ID（FR-n）／設計の節番号（§n）／該当コードの1行 のいずれか]
  - 署名: [R-001|欠陥類型|根拠ハッシュ（sha256 先頭8桁）]
  - 対応: [上記の固定語彙のいずれか]

## レビュー・修正ログ
<!-- W-2: `### ループ N` は追記のみ。過去ループのブロックで書き換えてよいのは `- ループ状態:` の値だけ -->

### ループ 1（[日時]）
- ループ状態: [実行中 / 完了 / 中断]
- レビュー体制: [同期 Agent 委譲（[n] 体） / 自己レビュー（degrade: [理由]） / レビュー未実施]
- プロファイル: [観点1体 / 観点3体 / なし（レビュー未実施）]
- 丸め: [なし / [localId] の [キー] を [値] へ丸めた]
- 委譲: 送出 [n] 体 / 受領 [m] 体 / 未受領 [k] 体 / リトライ計 [r] 回
- 未確認範囲: [パス一覧 / なし]
- 署名: [R-001|類型|hash, ...（必須かつ未解消のみ・辞書順・`, ` 連結。0件なら空）]
- 件数: 指摘 [n]（必須 [n] / 推奨 [n]）
- 新規: [R-nnn, ... / なし] ／ 解消: [R-nnn, ... / なし] ／ 未解消: [R-nnn, ... / なし]
- 停滞判定: [前ループなし / 前ループと差分あり / 前ループと同一 → 中断]
- 本ループで変更したファイル: [パス一覧 / なし]

#### 修正内容
- R-001: [変更ファイル] [修正の要旨。修正しなかった場合はその区分と理由]

## task-verify へ引き継ぐ事項
<!-- W-6: 上書き更新。静的レビューの結果を伝え、動的検証で同じ不具合を二重に直させない -->
- 静的レビューで解消済み: [R-nnn, ... / なし]
- 未解消のまま残した指摘: [R-nnn（保留 / 却下 / 予算超過）, ... / なし]
- 動的検証で重点確認したい範囲: [変更の影響範囲・確認観点]

## 終了サマリ
<!-- W-6: 上書き更新。W-9: 指摘0件でも `- 総合判定:` と `### ループ 1` は必ず書く -->
- 総合判定: [修正不要 / 修正推奨 / 修正必須 / 未実施]
- 内訳: 必須 [n] 件（未解消 [n] 件）/ 推奨 [n] 件 / 保留 [n] 件 / 却下 [n] 件
- 検査範囲: [全範囲を検査 / 未確認 [n] ファイル（下記「未確認範囲」に一覧） / レビュー未実施のため不明]
- 終了区分: [全指摘解消 / 修正推奨のみ残存 / ループ上限到達 / 停滞による早期中断 / 退行検出 / 無変更による中断 / 修正予算超過 / レビュー未実施（委譲不成立） / レビュー範囲不完全]
- 実行ループ数: [N] / 2
- 未解消の修正必須: [R-nnn, ... / なし]
- 保留・却下: [R-nnn（保留（要判断））, R-nnn（却下（J-2））, ... / なし]
- 区分を引き下げた指摘: [R-nnn（必須 → 推奨・J-n: 根拠）, ... / なし]
- 要件・設計の変更が必要なため未対応: [R-nnn, ... / なし]
- 未確認範囲: [パス一覧 / なし]
- 委譲の degrade: [なし / 未受領により自己レビュー / 委譲不成立]
- プロファイル: [観点1体 / 観点3体 / なし（レビュー未実施）]
- 委譲統計: 送出 [n] 体 / 受領 [m] 体 / 未受領 [k] 体 / リトライ計 [r] 回

<!-- W-1: `## メタ情報` は常に最終セクション。追記は必ずこの直前へ挿入する。
     W-8: Agent Teams は成立2条件のみで決まる。--team 未指定時の委譲は成立条件に含めない -->
## メタ情報
- 実行モデル: [モデル名]
- 実行日時: [日時]
- Agent Teams: [有効 / 無効（指定なし） / 無効（フォールバック）]
